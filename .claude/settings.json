{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If the file being edited is .claude-plugin/marketplace.json or .gitignore, BLOCK with reason 'Protected file — edit manually if needed'. Otherwise ALLOW."
          }
        ]
      }
    ]
  }
}
